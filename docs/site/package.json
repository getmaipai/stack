{
  "name": "stack-site",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "sync": "node scripts/sync-content.mjs",
    "sync:install": "node scripts/sync-install.mjs",
    "dev": "bun run sync && bun run sync:install && astro dev",
    "start": "bun run sync:install && astro dev",
    "build": "bun run sync && bun run sync:install && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.2.10",
    "sharp": "^0.35.3",
    "starlight-openapi": "^0.26.2"
  }
}
