# Lane B work order: the data behind the dashboard, then the templates, Overview and Models (2026-09-19)

**First message back (before any work):** `ready UI-B`, the model named
in your own system prompt (or "unknown"), your checkout and branch, and
"Lane B: UI-06, UI-07, UI-08, UI-09, then UI-10, UI-11, UI-12". Work
starts on the coordinator's reply `start`. The coordinator is the
session named `getmaipai-47 [486133]` (ListAgents; SendMessage with
`notify_when_idle: true` on every report).

## Who you are

Session B, the data and templates lane of the program in
[`ui-reconcile-2026-09-19.md`](ui-reconcile-2026-09-19.md) (read it
whole, then the spec at [`ui-spec-2026-09-19/spec.md`](ui-spec-2026-09-19/spec.md)
sections 3, 4, 5, 6 and the browser, Models and reuse sections, and
open `overview-dashboard.png` and `models-browser-and-pane.png`).
Model floor: Sonnet for the data items, Opus preferred for UI-10
onward (it spans every page). Speed is the owner's priority: one
commit per item, reported when green, no polish past the acceptance.

Setup: a worktree, because Lane A is editing the same repo on `main`:
`git -C ~/Developer/github.com/getmaipai/stack worktree add
~/Developer/github.com/getmaipai/stack-b -b b/ui-data main`, then
`bun install --silent` in `stack-b/backend` and `stack-b/frontend`.
Port 8771 for any live daemon you start (`PORT=8771`), and
`STACK_DATA_DIR=/tmp/claude-501/stack-b-data` for it and for every gate
(never the checkout's `data/`). Never run git that changes state in
the main checkout. You commit on your branch; Lane A lands your
commits on `main` on the coordinator's order (you report the hash).

Files you own: `backend/src/routes/network.ts`, `series.ts`,
`components.ts`, `activity.ts`, `notifications.ts`, `updates.ts`
(the recommendations route only), `backend/src/lib/network.ts`,
`series.ts`, `components.ts`, `activity.ts`, `notifications.ts`, the
tests beside them, `backend/src/app.ts` (route registration lines
only), `docs/api/openapi.json` (generated), `docs/user/privacy.md`
(one row), `docs/dev/session-b.md`, the BACKLOG lines of your items;
from UI-10 on, `frontend/src/kit/blocks/browser/**`,
`kit/blocks/pane/**`, `kit/blocks/cards/**`, `kit/hooks/**` (new),
`frontend/src/pages/OverviewPage.tsx` and `pages/overview/**`,
`pages/ModelsPage.tsx`, `lib/format.ts`, `lib/status.ts` (new).
Forbidden: everything Lane A owns (tokens, icons, the dashboard shell
components, `DashboardShell.tsx`, `App.tsx`, `taxonomy.ts`),
`SettingsPage.tsx` and `pages/settings/**` (Codex). When you need
`taxonomy.ts` before Lane A's UI-05 lands, import it by path and
report; do not write your own list.

## UI-06: `GET /stack/v1/network` (S)

`lib/network.ts`: the default interface (macOS `route -n get default`
for the interface and gateway, Linux `/proc/net/route`), link speed
(macOS `ifconfig <if>` media line or `networksetup -getmedia`, Linux
`/sys/class/net/<if>/speed`), gateway latency (one TCP connect or ICMP
round trip to the gateway with a 1 s timeout, three samples, the
median), each `null` when not measurable, cached 30 s, injected
readers like `lib/live.ts` (`LiveReaders`) so the test scripts the
command output. Route: `{ interface, linkMbps, gatewayMs, measuredAt }`,
operator or client auth as `routes/live.ts` uses, registered in
`app.ts`, tagged "Monitoring". Privacy: one row in `docs/user/privacy.md`
saying the Stack measures the round trip to your own router and
nothing beyond it. Tests: macOS and Linux scripted outputs, nulls when
a command fails. Commit `Monitoring (UI-06): the network signal`.

## UI-07: resource series (M)

`lib/series.ts` today records usage, memory and speed with ranges
hour, day, week (`readSeries`, `recordMemorySample`). Add: a sampler
that every 60 s records one `resources` sample: CPU percent (from the
live sampler's `cpu.percent`, `lib/live.ts`), memory used and total,
each GPU's utilization and memory (`gpus[]`, index and name kept), each
mounted drive's used and total (`drives[]`); start it where the memory
sampler starts (find `recordMemorySample`'s caller in `supervisor.ts`
or `app.ts`; reuse the live sampler's cadence rather than a second
`ps` run, `getLiveSample` or its equivalent is the source). Add
`"month"` to `SeriesRange` and `durations`. Route `GET
/stack/v1/series/resources?range=hour|day|week|month` returns per
resource a bucketed array (`hour`: 60 one-minute points; `day`: 96
fifteen-minute points; `week`: 168 hourly points; `month`: 120
six-hour points) with `null` where no sample exists (empty buckets stay
visible, spec section 6 "no false precision"), plus `devices` (GPU
names and drive names) so the UI can draw one row per device.
Retention: delete resource samples older than 35 days in the existing
maintenance pass (`lib/maintenance.ts`). If the two-database split
(STACK-50) is not built, use the existing series table with a `kind`
column; do not build STACK-50 here. Tests: a scripted live sample
records the row; bucketing keeps nulls; month range exists; retention
deletes. Commit `Monitoring (UI-07): resource history for every device`.

## UI-08: the components aggregate, activity, notifications (M)

`lib/components.ts` builds one list from the real stores per program
decision 1 (the table in section 3): models from the model store (kind
to subtype: LLMs, Image, Video, Audio, Embeddings; size, quantization
from the GGUF reader if present, runtime = the engine that serves it,
status from the governor's loaded set and the role states, last used
from usage samples); runtimes from the engine records and the detected
installs (`detect.ts`; a detected, unadopted install is status
"detected"); apps from the clients holding `coding` (Coding) or any
role (Agents when the client name or preset says a harness; otherwise
Coding), Chat and Image as the two Tester surfaces with status from the
roles, Knowledge as the Library; extensions from the alert channels and
the mirror setting (Integrations) and the served MCP server (MCP
Servers); system from hardware (Accelerators: each GPU or the Apple
chip; Drivers: Metal or the CUDA driver version; Dependencies: each
engine build pin). Adapters, Workflows, Training: empty arrays, and the
route says `"managed": false` for those categories so the UI shows the
designed empty state. Row shape (Zod, exported): `{ id, category,
subtype, name, identifier, version, sizeBytes, status
("running"|"stopped"|"detected"|"update"|"warning"|"error"|"ready"),
statusText, runtime, resources { memoryBytes, gpuPercent, cpuPercent },
uptimeSeconds, lastUsedAt, notes }` where each nullable. Route `GET
/stack/v1/components?category=&status=` and `GET
/stack/v1/components/summary` `{ installed, running, clientsConnected
(clients with lastSeenAt in the last 24 h, split local and remote by
their last address if recorded, else all local), updatesAvailable,
perCategory { models: n, … } }`. `GET /stack/v1/activity`: durable
events (model installed or loaded, engine started or stopped, update
available, check completed, health item raised or cleared) from the
events log if one is persisted (`lib/events.ts`; if events are not
persisted, add a small `activity` table written by the same emit path,
last 200 kept). `GET /stack/v1/notifications` and `POST
/stack/v1/notifications/{id}/read`: unread actionable items = health
items at warning or above plus available updates, read state kept per
item id in the settings store. Tests: each category's rows from
scripted stores; the summary counts; filters; activity written on a
model install; a notification read stays read. Commit `Console (UI-08):
one component list, activity and notifications`.

## UI-09: recommendations (S)

`GET /stack/v1/updates/recommendations`: rows `{ id, category, name,
currentVersion, availableVersion, reason, action ("update"|"install"|
"view"), sizeBytes }` merging the app, engine and model update state
(`updates/check.ts`, STACK-31) and the fitting-models recommendation
(STACK-21, `updates/models.ts`, landed by Lane A's Step 0; base on
`main` after Lane A reports it pushed). Reasons are evidence-led
sentences in the spec's voice ("Fits in 12 GB of free memory beside
chat" and the like). Tests: the merge and the reason strings. Commit
`Updates (UI-09): recommendations as one list`. Report `done UI-B data`
with the four hashes; Lane A lands them.

## UI-10: the shared templates and hooks (M, after Lane A reports UI-02 pushed; rebase your branch onto that main first)

Build once, in `frontend/src/kit/blocks/` and `kit/hooks/`, on the
kit's shadcn primitives (`kit/ui/*`: tabs, table, sheet, dialog,
dropdown-menu, tooltip, command, skeleton, toggle-group, select,
checkbox, popover) with the tokens Lane A landed (no raw colors):

- `browser/CategoryBrowser.tsx`: the sticky controls dock (mode tabs
  Installed, Browse, Updates, Recommended; subtype facets from the
  taxonomy with counts, horizontally scrollable under 960; Filter,
  Sort, list/grid switch; the primary action), the list or grid below,
  state in the URL search params so it survives the pane opening and
  closing.
- `browser/DataTable.tsx` on `@tanstack/react-table` (installed):
  sticky column header under the dock, a selection checkbox column, a
  three-dot row menu, safe truncation with a tooltip, row click selects
  and opens the pane, arrow keys move the selection, secondary columns
  hidden under 1280 by a column priority.
- `pane/DetailsPane.tsx`: 560 px (min 480, max 640), 16 to 20 px inset,
  16 px radius, thin blue border, the shadow token, dim overlay that
  leaves the browser recognizable; open 200 ms translate 24 px and
  fade, close 160 ms, none under reduced motion; the header (category
  tile, name, identifier, status pill, close), tabs shown only when
  the caller passes content for them, the action rail (a list of
  `{ label, icon, onClick, destructive?, disabledReason? }`, destructive
  ones separated in red and routed through `ConfirmDialog`), focus to
  the heading on open and back to the row on close, `role="complementary"`
  with a label; a full-width overlay under 960 and a full-screen sheet
  with Back and sticky actions under 720.
- `ConfirmDialog.tsx` (what is affected: files, dependents; the safe
  default), `states/` (Empty with one sentence and one action, Loading
  as skeletons matching the final geometry, Error with a recovery
  action), `cards/MetricCard.tsx` (outlined neon icon tile, count with
  tabular figures, label, state line or link), `cards/ResourceRow.tsx`
  (icon tile, label, percent, bar, 1.5 px sparkline from a series with
  nulls as gaps, capacity label, tooltip with timestamp, value and
  device), `cards/CategoryTile.tsx`, `cards/ActionTile.tsx`,
  `cards/StatusPill.tsx`.
- Hooks and utilities: `useSearchFocus` (⌘K and `/`), `useFacetSort`
  (debounced filter, sort, facet in search params), `useSelectionPane`
  (one open pane, replace on another row, close on Escape and same
  row), `lib/status.ts` (status to hue, dot, label and pill class, one
  map), `lib/format.ts` (bytes, percent, rates, durations, versions,
  relative time with the absolute in a `title`), `useBreakpoint`
  (720, 960, 1280 from the tokens), `useFocusReturn`, `useConfirm`.
  Delete `things-table`, `things-page`, `property-panel` and
  `filter-column` when nothing imports them (after UI-12), never before.

Tests beside each (render, keyboard, focus return, hidden empty tabs,
reduced motion path, the status map exhaustively). Commit `Console
(UI-10): the shared browser, pane, cards and hooks`. Report `done
UI-10`; Codex and Session C build on it.

## UI-11: Overview (M)

Spec section 3 on UI-10 over `/stack/v1/components/summary`,
`/stack/v1/series/resources`, `/stack/v1/components?status=running`,
`/stack/v1/activity`, `/stack/v1/updates/recommendations`: the four
metric cards; System Resources (CPU, Memory purple, GPU teal with the
device name or the Apple accelerator, Storage orange, each a
`ResourceRow`; the 1h / 1D / 1W / 1M control; "View All Resources" to
`/monitoring`); Active Components (Name, Type, Status, Resources,
Uptime, Actions; "View All" violet to `/packages?mode=installed&status=running`);
Installed Components tiles (eight, counts, "Browse All" to
`/packages`); Recent Activity (dots by kind, relative time with the
absolute in the tooltip, "View All" to `/logs`); Updates &
Recommendations (badge count, rows with Update, Install or View);
Quick Actions (Install Component to `/abilities`, Check for Updates
posts `/stack/v1/updates/check`, Run Speed Test opens the existing
speed test flow, View Logs to `/logs`). Equal-height cards; the
responsive tiers from spec section 7. The board's first-run state
(program decision 8) renders here when no plan exists (`BoardPage
embedded`, keep it). Captures at 1440, 1100, 800, 390 opened and
judged against `overview-dashboard.png`. Commit `Console (UI-11):
Overview to the approved composition`.

## UI-12: Models on the browser (M)

`ModelsPage.tsx` becomes a `CategoryBrowser` configuration: columns
Name (with the tinted icon tile), Type, Size, Quantization, Status,
Runtime, Last used, Notes, Actions; facets All, LLM, Image, Video,
Audio, Embedding; Installed from `/stack/v1/components?category=models`,
Browse from the catalog route, Updates and Recommended from UI-09;
Install Model opens the existing Add sheet; the pane's Overview tab
(description, capability tags, type, size, quantization, context
length, architecture, licence, runtime, installed, last used,
location; resource cards memory, GPU, tokens/s from the latest speed
result), Settings (the model's declared settings through the generic
form), Usage (the usage series for this model), Files (the store's
files with sizes), Logs (the serving engine's tail); action rail Chat
(to `/try?model=`), Load or Stop, Restart, Update, Test, Open logs,
Remove (confirm names the files and the roles that lose the model).
The spec's "Models page scrolling and pinned regions" is the
acceptance: rail, header and footer pinned; the dock and column header
sticky; only rows scroll; state survives the pane. Captures: the list
and the pane open at 1440, the overlay at 800, the sheet at 390,
judged against `models-browser-and-pane.png`. Commit `Console (UI-12):
Models on the shared browser`. Report `done UI-B`.

## Exit checks, every item

`STACK_DATA_DIR=/tmp/claude-501/stack-b-data bash scripts/check.sh` in
the worktree, in the background (a foreground run is killed at 120 s);
`code-review` at medium with the explicit target
`~/Developer/github.com/getmaipai/stack-b` and the reported path
checked before acting on a finding; docs in the same commit (BACKLOG
tick with its status line, the privacy row, `docs/dev/session-b.md`);
`bun run gen:api-docs` in `backend/` before the gate when a route
changed; stage by file name after `git status`; `git show --stat HEAD`
read before reporting; servers recorded by pid and port and stopped by
pid. Report `ready`, `done <item>` (hash, commands and results, capture
paths), `blocked <item>` (the exact error, what you tried, your guess at
the class), `question <item>`, `low context` (early).
