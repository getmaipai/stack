# Lane A work order: land the in-flight work, then the shell foundation (2026-09-19)

**First message back (before any work):** `ready UI-A`, the model named
in your own system prompt (or "unknown"), your checkout and branch, and
"Lane A: landing plus UI-05, UI-01, UI-02, UI-03, UI-04". Work starts
on the coordinator's reply `start`. The coordinator is the session named
`getmaipai-47 [486133]` (find it with ListAgents; message it with
SendMessage, `notify_when_idle: true` on every report).

## Who you are

Session A, the integrator and the foundation lane for the program in
[`ui-reconcile-2026-09-19.md`](ui-reconcile-2026-09-19.md) (read it
whole, then the spec at [`ui-spec-2026-09-19/spec.md`](ui-spec-2026-09-19/spec.md)
and open its six images). You work in the main checkout
`~/Developer/github.com/getmaipai/stack` on `main`, port 8770,
`STACK_DATA_DIR` left unset for the live app but set to a temp dir
for every gate (`STACK_DATA_DIR=$(mktemp -d) bash scripts/check.sh`;
a gate on the operator's real `data/` once emptied it). Model floor:
Opus (the shell spans every page). Speed is the owner's priority
tonight: one commit per item, pushed as soon as green, no polishing
beyond the acceptance.

Files you own: `frontend/src/kit/tokens.css`, `frontend/src/kit/icons.ts`,
`frontend/src/kit/blocks/dashboard/**`, `frontend/src/kit/blocks/phone/**`
(to delete), `frontend/src/lib/taxonomy.ts` (new), `frontend/src/pages/DashboardShell.tsx`,
`frontend/src/App.tsx`, `frontend/src/pages/LoginPage.tsx` (the Locked
wording only), the tests beside them, `docs/BACKLOG.md` UI lines,
`docs/ux.md` (pointer lines only), `docs/dev/session-a.md` (your notes).
Forbidden: `backend/**` (Lane B's), `frontend/src/pages/SettingsPage.tsx`
and `pages/settings/**` (Codex), `frontend/src/kit/blocks/things-*`,
`property-panel`, `filter-column` (Lane B replaces them in UI-10).

## Step 0: land the seven in-flight commits (do this first, report when pushed)

Reports are in `~/Developer/github.com/getmaipai/home/data-scratch/c/queue/done/`
(`c-65c-*-REPORT.md`, `c-67-*.md`, `codex-123|125b|126|127|128-*-REPORT.md`).
Cherry-pick onto `main` in this order, one at a time, never chained:

1. `a1de9c8` (`c/67-stack-weekly-digest`, Digest STACK-27)
2. `bd926ec` (`c/67-stack-desktop-setup`)
3. `a5c408e` (`codex/123-stack-storage-hygiene`)
4. `4c108e0` (`codex/125b-stack-guided-fixes`)
5. `8824de3` (`codex/126-stack-connect-a-coding-tool`)
6. `aea34be` (`codex/127-stack-ready-when-you-sit-down`)
7. `5bde079` then `96552e7` (`codex/128-stack-whats-new-for-your-computer`)

A conflict you can resolve by keeping both intents, resolve (you are a
coder session; the no-conflict rule binds the coordinator). Then one
gate on the combined `main` in the background (`STACK_DATA_DIR=$(mktemp -d)
bash scripts/check.sh > /tmp/claude-501/gate-a.log 2>&1 &`, the
gate takes several minutes; a foreground run is killed at 120 s and
looks like a failure). Known flake: `hardware.test.ts` "caches within
its TTL" compares live free-disk bytes; if it is the only red, run it
alone once and record both results. `docs/api/openapi.json` drift is
fixed by `bun run gen:api-docs` in `backend/` and staging the result
into the pick that caused it (amend). Green: `git push`, then delete
every lane branch whose commit is now on main (`git branch -D` for the
seven above and the 20 already-landed branches `git branch --merged`
and the cherry-picked ones listed in the program note's section 5; a
branch whose subject matches a main commit is landed). Do not regenerate
captures. Report `done UI-A step 0` with the push hash and the gate
line, then continue without waiting.

## Step 1: UI-05, the taxonomy declaration (S)

`frontend/src/lib/taxonomy.ts` exports `groups` (STACK, System,
Resources, MANAGE) each with its destinations `{ id, label, path,
subtitle, icon, hue?, subtypes? }` exactly as spec section 2 lists
them, in that order, and `categories` (the eight, with icon and hue
from spec section 4). Paths: keep today's where they exist (`/`,
`/models`, `/clients`, `/monitoring`, `/try`, `/settings`, `/logs`,
`/alerts`, `/docs` replaces `/help` and `/library` with redirects,
`/runtimes` replaces `/engines` with a redirect); new: `/adapters`,
`/apps`, `/workflows`, `/extensions`, `/training`, `/system`,
`/packages`. Subtitles: one short line each in the spec's voice
("All systems, components, and resources at a glance." for Overview;
"Manage, install, and run AI models" for Models; write the rest).
Test `taxonomy.test.ts`: the labels and subtypes equal the spec's
lists verbatim (put the expected arrays in the test, typed from the
spec), and a grep test that no other file under `frontend/src`
declares the destination list (search for `"Adapters"` outside
taxonomy.ts and its test). Commit `Console (UI-05): one taxonomy
declaration`. Push.

## Step 2: UI-01, the token set (M)

`kit/tokens.css`: the 13 hex values of spec section 1 become the dark
root (`.dark`) values, mapped onto the kit's variables (`--surface-page`
canvas, `--surface-sidebar`, `--card`/`--surface-card` panel,
`--accent`/`--surface-pane` raised panel, `--border`, `--foreground`,
`--muted-foreground`, `--primary` electric blue, plus new
`--hue-violet`, `--hue-teal`, `--hue-orange`, `--hue-pink`,
`--hue-red`, and `--cat-models` … `--cat-system` per spec section 4);
radii `--radius-card: 16px`, `--radius-control: 12px`, pills 999;
`--shadow-panel: 0 12px 28px rgba(0,0,0,.24)`; focus ring 2 px
`#21A6FF` with offset; the type scale as utilities or variables
(`--text-title 30px/600`, section 18px/600, metric 28px/600 tabular,
body 13.5px/500, supporting 12px/400); breakpoints `--breakpoint-md:
720px`, `--breakpoint-lg: 960px`, `--breakpoint-xl: 1280px` (keep `sm`
640 for the existing primitives; `kit/responsive.ts`, if it exists,
gets the same numbers and its test). The light root: the same hues on
light surfaces (canvas `#F4F7FB`, sidebar `#EAF0F7`, panel `#FFFFFF`,
raised `#E3EAF3`, border `#C9D6E6`, text `#0B1730` and `#4A5F7A`),
program decision 5. Delete the "dark is black" and "accent is orange"
comments; the header comment names the spec as the source. Tests:
`tokens.test.ts` asserts the 13 dark values; a new `contrast.test.ts`
computes WCAG contrast for primary and secondary text on canvas,
sidebar, panel and raised panel in both themes and asserts 4.5 (or 3.0
for the 12 px supporting text on raised panel if it fails, recorded).
Commit `Console (UI-01): the approved token set`. Push. Open one capture
of Overview after this commit and judge that nothing became unreadable
(the layout is still the old one; that is expected).

## Step 3: UI-02, the shell (M, the big one)

Rebuild `kit/blocks/dashboard/components/*` and `DashboardShell.tsx`
on the existing sidebar primitive (`kit/ui/sidebar.tsx`, which has the
icon-collapsible mode) to the spec's "Application shell standards",
"Left navigation rail", "Fixed top header", "Fixed footer", "Current
destination header rule" and "Shell consistency rules":

- Rail: 252 px expanded, 72 px collapsed; the stacked mark and
  wordmark with the subline "Your AI. On Your Terms."; a double-chevron
  toggle beside the wordmark (44 px target, keyboard, `aria-expanded`),
  state persisted in `localStorage` under `maipai-stack:rail`; the four
  groups with uppercase labels and dividers from `taxonomy.ts`; active
  item violet fill and white label; collapsed: icons only, tooltips,
  dividers kept, labels hidden. Default collapsed at 960 to 1279 px,
  expanded at 1280 and up (only until the person toggles). Under 720
  px: an off-canvas drawer (`kit/ui/sheet.tsx`) opened by a header
  button, the pulse in its footer. The bottom block is a placeholder
  `SystemPulse` slot filled in Step 4.
- Header: 64 px, fixed across the content region; left the destination
  icon, title and subtitle from the taxonomy for the current route
  (updates on every navigation; never a static "Overview"); center a
  real `<input>` with the placeholder "Search models, apps, drivers,
  anything…" and a ⌘K hint that focuses it (`/` too); for this item
  the results below the field are the existing palette's groups
  rendered in a popover anchored to the field (UI-16 completes the
  result kinds; the `CommandDialog` modal is removed now); right the
  appearance control (System, Light, Dark; `next-themes` is installed),
  the notifications bell with a red badge only when the count is
  greater than zero, and a `MachineSelector` slot filled in Step 5. No
  run pill, no profile menu (program decision 2 and 3).
- Footer: 40 px fixed; left "MaiPai Stack v{version}" from
  `GET /stack/v1/health`'s `version`; center three linked counts
  ("{n} updates available" to `/settings/updates`, "{n} components
  installed" to `/packages?mode=installed` for now, "{n} running" to
  `/monitoring`), from today's routes (updates count as the shell
  computes it; installed = models + engines that match this machine;
  running = engines in a ready or busy state plus loaded models; UI-08
  replaces the source later, keep the computation in one hook
  `useStackCounts`); right the health dot and text ("All systems
  operational", or the worst health item's text in amber or red, or
  "Paused by you" while paused). Hidden counts at narrow widths in the
  order the spec gives; never two rows.
- Content region: between header and footer, the only scroller, 24 px
  padding at desktop. `SectionFrame` loses its description line (the
  header owns the subtitle); pages that render their own `<h1>` title
  keep working but the shell test asserts no page renders a heading
  equal to the destination label (fix the ones that do: Monitoring,
  Logs, the phone routes).
- Remove: `PhoneShell`, `TabBar`, `PhoneHeader`, `PhoneModelRoute`,
  `PhoneEngineRoute`, `ProfileMenu`, the run pill, the "This computer"
  header item; `phone.test.tsx` and `onePill.test.tsx` are replaced by
  the new shell tests. Pause everything: keep the tray listener and add
  a "Pause everything" / "Resume" command to the search results'
  commands group; UI-15 adds the Settings control.
- Reduced motion: no transitions under `prefers-reduced-motion`.

Tests (`dashboardShell.test.tsx`, rewritten): the rail renders the
17 labels in order under the four group labels; the toggle collapses
and persists; the header title and subtitle change when the route
changes to `/models` and `/settings`; ⌘K focuses the search input;
the footer shows the version and three counts; no duplicate heading
on `/models`; under 700 px the drawer button exists and the rail does
not. Captures: `bun scripts/screenshot.ts --showroom` in the background
(it exceeded a foreground window once) at 1440, 1100, 800 and 390 of
Overview; open each and judge against `overview-dashboard.png` for the
shell only (the Overview content is UI-11's). Commit `Console (UI-02):
the approved shell`. Push. Report `done UI-02` with the capture paths;
Lane B and Codex rebase onto this commit.

## Step 4: UI-03, the system pulse (S)

`kit/blocks/dashboard/components/system-pulse.tsx`: five icon buttons
in order (Stack health: activity glyph; Memory: chip; Storage:
database; GPU: a GPU glyph; Network: gauge), each with a semantic dot
(green, amber, red, muted) and an accessible name; Stack health with
a red badge = count of health items at error or critical (from
`/stack/v1/health`), GPU with a blue badge = detected GPU count (from
`/stack/v1/live` `gpus.length`, or 1 on Apple silicon from
`/stack/v1/hardware`); dots: memory from `/stack/v1/budget` pressure
(normal green, warn amber, critical red), storage from the smallest
free share across `/stack/v1/storage` drives (amber under 15%, red
under 5%), GPU green when a sample exists and muted when none,
Network muted until `GET /stack/v1/network` exists (Lane B's UI-06;
when it does: green, amber above 50 ms, red when the gateway does not
answer). Tooltips carry the detail sentence from the spec's examples.
Clicks: health to `/alerts?severity=error`, the rest to
`/monitoring#memory|storage|gpu|network`. Test each dot and badge
from scripted responses. Capture the rail expanded and collapsed.
Commit `Console (UI-03): the system pulse`. Push.

## Step 5: UI-04, the machine/stack selector and Lock (S)

`kit/blocks/dashboard/components/machine-selector.tsx` on the
dropdown-menu primitive: the closed control (monitor icon, health dot,
`hardware.computerName` or "This computer", chevron); the menu:
"Current stack" heading, the selected row (name, `osVersion` and the
health sentence, dot, violet selected background), a "Switch stack"
section rendered only when a remote list exists (none today; leave a
typed empty array from a `useRemoteStacks` hook that returns `[]`,
UI-22 fills it), a divider, then Lock MaiPai (POST
`/stack/v1/operator/logout`, then the login card; `LoginPage.tsx`
reads "Locked" with one sentence "Enter the operator password to
unlock. The Stack keeps running." when the state came from a lock,
and its existing wording otherwise), Settings (`/settings`), Help
(`/docs`). Remove every other sign-out control (grep `logout` in
`frontend/src`). Tests: the menu items in order; Lock calls the route
and shows the Locked card. Capture the open menu against
`machine-stack-selector.png`. Commit `Console (UI-04): the machine
selector and Lock MaiPai`. Push. Report `done UI-A` with the whole
list of commits, gates and capture paths.

## Exit checks, every item

`STACK_DATA_DIR=$(mktemp -d) bash scripts/check.sh` in the background,
green; the `code-review` skill at medium on the diff about to be
committed (you are in the main checkout, so the reported path is
yours); findings fixed or dispositioned in the report; docs in the
same commit (the BACKLOG tick with its status line, `docs/dev/
session-a.md` for anything worth recording); stage by file name after
`git status`; read `git show --stat HEAD` before reporting. Any server
you start is recorded by pid and port and stopped by pid.

## Reporting

`ready`, `done <item>`, `blocked <item>` (the exact error, what you
tried, your guess at the class), `question <item>`, `low context`
(early, with what is in the tree). Messages go to the coordinator
session named above. After Step 5 you become the integrator for Lane
B, Codex and Session C: the coordinator sends you each landing order
(branch, hash, gate) and you land in program order.
