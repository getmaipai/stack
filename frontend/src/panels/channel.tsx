import type { ReactNode } from "react";
import type { PropertyAction } from "@/kit/blocks/property-panel/PropertyPanel";
import { KeyValueList } from "@/kit/blocks/property-panel/KeyValueList";
export interface ChannelPanelResult { actions: PropertyAction[]; primaryActions: [PropertyAction, PropertyAction]; facts: Array<{ label: string; value: ReactNode }>; overview: ReactNode }
export function channelPanel(type: string, verified: string, onAction: (action: string) => void): ChannelPanelResult { const test: PropertyAction = { label: "Send a test", icon: "Send", onClick: () => onAction("test") }; const edit: PropertyAction = { label: "Edit", icon: "Settings", onClick: () => onAction("edit") }; return { actions: [test, edit], primaryActions: [test, edit], facts: [{ label: "Type", value: type }, { label: "Verified", value: verified }, { label: "Scope", value: "Local" }], overview: <KeyValueList items={[{ label: "Kind", value: "Alert channel" }, { label: "Type", value: type }, { label: "Verified", value: verified }]} /> }; }
