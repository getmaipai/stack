{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/shared/spec/schemas/voice.schema.json",
  "title": "Voice",
  "description": "One voice the tts role can render, as listed by GET /stack/v1/voices: its id, the friendly name, the declared metadata (description, language, country, gender) with unknown where the source declares none, the source kind, whether the embedding is on disk, the licence, and the revision.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "description", "language", "country", "gender", "source", "onDisk", "licence", "revision"],
  "properties": {
    "id": { "type": "string", "minLength": 1 },
    "name": { "type": "string", "minLength": 1, "description": "The friendly name for a picker." },
    "description": { "type": "string", "minLength": 1, "description": "Declared, never guessed; unknown when the source declares none." },
    "language": { "type": "string", "minLength": 1, "description": "BCP 47 or unknown." },
    "country": { "type": "string", "minLength": 1, "description": "ISO 3166-1 alpha-2 or unknown." },
    "gender": { "type": "string", "enum": ["female", "male", "neutral", "unknown"] },
    "source": { "type": "string", "enum": ["preset", "community", "cloned"] },
    "onDisk": { "type": "boolean" },
    "licence": { "type": ["string", "null"] },
    "revision": { "type": "string", "minLength": 1 }
  }
}
