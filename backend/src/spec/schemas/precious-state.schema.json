{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://getmaipai.github.io/shared/spec/schemas/precious-state.schema.json",
  "title": "PreciousState",
  "description": "What the Stack declares for Home's backup (GET /stack/v1/backup): its data directory and, for each path under it, whether to include or exclude it and why. Home takes, encrypts, schedules and restores the backup (org BACKUPS.md); the Stack only declares.",
  "type": "object",
  "additionalProperties": false,
  "required": ["data_dir", "paths"],
  "properties": {
    "data_dir": { "type": "string", "minLength": 1 },
    "paths": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["path", "mode", "what", "why"],
        "properties": {
          "path": { "type": "string", "minLength": 1 },
          "mode": { "type": "string", "enum": ["include", "exclude"] },
          "what": { "type": "string", "minLength": 1 },
          "why": { "type": "string", "minLength": 1, "description": "The sentence Home shows beside the path." }
        }
      }
    }
  }
}
